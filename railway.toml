[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --worker-class gthread --workers 2 --threads 4 --timeout 90 --keep-alive 30 --bind 0.0.0.0:$PORT nexus_omega_v14:app"
healthcheckPath = "/ready"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
