[build]
builder = "nixpacks"

[deploy]
startCommand = "python nexus_omega_v8.py"
healthcheckPath = "/ready"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[[services]]
name = "nexus-omega"
